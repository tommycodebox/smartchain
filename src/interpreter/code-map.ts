const STOP = 'STOP'
const ADD = 'ADD'
const SUB = 'SUB'
const MUL = 'MUL'
const DIV = 'DIV'
const PUSH = 'PUSH'
const LT = 'LT'
const GT = 'GT'
const EQ = 'EQ'
const AND = 'AND'
const OR = 'OR'
const JUMP = 'JUMP'
const JUMPI = 'JUMPI'

export const CODE_MAP = {
  STOP,
  ADD,
  SUB,
  MUL,
  DIV,
  PUSH,
  LT,
  GT,
  EQ,
  AND,
  OR,
  JUMP,
  JUMPI,
}
